import { View, Image } from "react-native"
import { appLogo } from "../utils/images";

const HomeScreen = () => {
    useEffect(() => {
        SplashScreen.hide();
    }, []);
    return (
        <View>
        </View>
    )
}
export default HomeScreen;